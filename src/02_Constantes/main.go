package main

import "fmt"

func main() {
	// Declaración de una constante
	const pi float64 = 3.1415
	// Las constantes tambien pueden ser declaradas sin indicar el tipo(ejem.float64) pero no es lo recomendable

	// Println es Usado para que al imprimir haga un salto de linea al final
	fmt.Println("El valor de pi es", pi)
}
